package com.fullStack.user.service;

import java.util.List;

import com.fullStack.user.model.Media;
import com.fullStack.user.model.MediaDto;

public interface MediaService {

    Media save(MediaDto user);

    Media findOne(String username);

    MediaDto update(MediaDto userDto);
}
